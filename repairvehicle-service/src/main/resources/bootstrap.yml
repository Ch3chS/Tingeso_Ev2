spring:
  application:
    name: repairvehicle-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:postgresql://localhost:5432/repairvehicle
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
