spring:
  application:
    name: repairlist-service
  cloud:
    config:
      enabled: true
      import: "configserver:http://config-server-service:8888"
  jpa:
    hibernate:
      ddl-auto: update
