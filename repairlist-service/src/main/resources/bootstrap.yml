spring:
  application:
    name: repairlist-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:postgresql://localhost:5432/repairlist
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
