spring:
  application:
    name: vehicle-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:postgresql://localhost:5432/vehicle
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
